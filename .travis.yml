language: python
python:
    - "3.4"
install:
    - pip install -r requirements.txt
    - pip install -r dev_requirements.txt
    - pip install coveralls
script:
    - nosetests
after_success:
    - coveralls
