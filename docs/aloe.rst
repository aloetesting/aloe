Running Aloe
============

The ``aloe`` helper runs Nose_ with the Aloe_ plugins enabled.

``aloe`` accepts the same flags as ``nosetests`` and so these are not
extensively documented here.

.. program:: aloe

.. option:: <feature>

    Run only the specified feature files.
